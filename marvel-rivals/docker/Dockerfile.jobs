FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
# Default behavior: do nothing until you run it manually
CMD ["python", "upload_leaderboards_and_hero_stats.py"]
